import styled from 'styled-components'

const CardDate = styled.span`
  white-space: nowrap;
  font-size: 0.9rem;
  font-weight: 200;
  grid-row: -1;
  grid-column: 1 / -1;
  place-self: end;
`

export default CardDate
